defmodule UtilitiesDetectionTest do
  use ExUnit.Case
  doctest Utilities.Detection

  # Test `find_document` returns one of each type.
  # TODO: Test `find_document` has a problem parsing the document.
end
